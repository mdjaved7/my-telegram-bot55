services:
  - type: web
    name: my-telegram-bot55
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    buildpacks:
      - url: https://github.com/render-examples/heroku-buildpack-ffmpeg-latest.git
      - url: heroku/python
      
